#Write program that determine wheather a year enter by the User is a leap year year are not using if else statement
number=int(input())
if number%4==0:
  print("leap year")
else:
  ptint("not leap year")